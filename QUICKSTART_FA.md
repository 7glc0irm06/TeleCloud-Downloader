<div dir="rtl">

# شروع سریع TeleCloud-Downloader (مبتدی)

> نسخه انگلیسی: [QUICKSTART.md](./QUICKSTART.md)  
> راهنمای کامل نصب: [SETUP_FA.md](./SETUP_FA.md)

این راهنما برای کاربران مبتدی است که می‌خواهند روی Ubuntu/Debian با یک مسیر ساده نصب را انجام دهند.

## پیش‌نیازها

- سرور Ubuntu/Debian
- دسترسی اینترنت
- کاربر دارای `sudo`
- پروژه از قبل clone/extract شده باشد

## اجرای نصب‌کننده

از ریشه پروژه:

```bash
chmod +x start.sh
./start.sh
```

## اسکریپت چه کار می‌کند

- ابزارهای لازم سرور و Docker را نصب/بررسی می‌کند
- فایل‌ها و پوشه‌های ضروری را به‌صورت امن می‌سازد
- متغیرهای ضروری ربات را از شما می‌گیرد:
  - `DOWNLOADER_BOT_TOKEN`
  - `BOT_PASSWORD`
  - `TELEGRAM_API_ID`
  - `TELEGRAM_API_HASH`
  - `ADMIN_ID`
- مقدارهای پیش‌فرض امن برای تنظیمات پیشرفته ثبت می‌کند
- مسیر راه‌اندازی Google Drive (`rclone.conf`) را مدیریت می‌کند
- سرویس‌ها را با Docker Compose بالا می‌آورد

## نکته Google Drive

اگر در زمان نصب Google Drive را رد کنید، اسکریپت باز هم `./rclone.conf` را به‌صورت placeholder می‌سازد.  
این کار از کرش mount در Docker جلوگیری می‌کند و ربات را در حالت Telegram-only بالا می‌آورد.

## رفتار در اجرای دوباره

هر زمان می‌توانید `./start.sh` را دوباره اجرا کنید:

- مقادیر معتبر `.env` را حفظ می‌کند
- سیستم و فایل‌ها را دوباره بررسی می‌کند
- فقط برای موارد ناقص دوباره سوال می‌پرسد (مثلاً نبودن Drive config)

</div>

